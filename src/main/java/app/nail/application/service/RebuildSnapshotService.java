package app.nail.application.service;

import org.springframework.stereotype.Service;

/** English: Calls DB procedures to rebuild pending counters. */
@Service
public class RebuildSnapshotService {
    // English: inject JdbcTemplate later to call rebuild functions.
}
